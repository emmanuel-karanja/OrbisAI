FROM python:3.13-slim

WORKDIR /app

COPY ingest.py .

RUN pip install chromadb requests fitz markdown bs4 transformers langchain_textsplitter transformers pydantic fastapi sentencepiece torch

CMD ["uvicorn", "ingest:app", "--host", "0.0.0.0", "--port", "8001"]
